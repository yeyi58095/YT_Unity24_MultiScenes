using UnityEngine;

public class Hi_MyScene : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Hi, MyScene has been loaded!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
